// const url:"http://localhost:3000"|"https://sekndapp.com" = process.env.NODE_ENV === "development" ? "http://localhost:3000" : "https://sekndapp.com"

// export const useTicketMasterCall:() => any = async() => {
//    const [ticketmasterLoading, setLoading] = React.useState<boolean>(false)
//     const triggerTicketMasterCall:() => Promise<{[key:string]:any}[]|string> = async () => {
//         try{
//             fetch(`${url}/api/ticketmasterAPI`)
//         }
//         catch(error){

//         }
//     }
//     return {ticketmasterLoading, triggerTicketMasterCall}
// }
